# Python 3.10+ required.
#
# PyTorch is pinned to a CUDA 12.6 build. Install torch/vision/audio from the
# PyTorch index FIRST, then install the rest:
#
#   pip install torch==2.7.1+cu126 torchvision==0.22.1+cu126 torchaudio==2.7.1+cu126 \
#       --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements.txt
#
# For a CPU-only or different-CUDA setup, install the matching torch build
# (https://pytorch.org/get-started/locally/) and ignore the +cu126 pins below.

# --- Deep learning core ---
torch==2.7.1+cu126
torchvision==0.22.1+cu126
torchaudio==2.7.1+cu126

# --- Segmentation / classification frameworks ---
nnunetv2==2.6.2
segmentation_models_pytorch==0.5.0
timm==1.0.19

# --- nnU-Net dependencies (explicit pins for reproducibility) ---
batchgenerators==0.25.1
batchgeneratorsv2==0.3.0
acvl_utils==0.2.5
dynamic_network_architectures==0.4.2
connected-components-3d==3.24.0

# --- Scientific stack ---
numpy==2.2.6
scipy==1.16.1
scikit-learn==1.7.1
scikit-image==0.25.2
pandas==2.3.1

# --- Medical imaging I/O ---
nibabel==5.3.2
SimpleITK==2.5.2

# --- Image processing ---
Pillow==11.3.0
opencv-python==4.12.0.88
imagecodecs==2025.3.30
tifffile==2025.6.11
albumentations==2.0.8
einops==0.8.1

# --- Visualization / logging ---
matplotlib==3.10.3
seaborn==0.13.2
tensorboard==2.20.0
tqdm==4.67.1

# --- Spreadsheet export (metrics) ---
openpyxl==3.1.5

# --- Config ---
PyYAML==6.0.2
yacs==0.1.8

# --- Optional: few-shot UniverSeg inference (universeg_inference.py is NOT part of
#     this release, but the dependency is recorded for completeness). Uncomment to install:
# universeg @ git+https://github.com/JJGO/UniverSeg.git@833a0c34c65e38d675e21bd48ddec6797cc03259
